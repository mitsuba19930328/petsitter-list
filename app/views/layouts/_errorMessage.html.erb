
<!-- error Message出力（application.html.erb）で使用 -->
<div class="container">
  <% if flash.notice.present? %>
    <div class="alert alert-success"><%= flash.notice %></div>
  <% end %>
  <% if flash.alert.present? %>
    <div class="alert alert-warning"><%= flash.alert %></div>
  <% end %>
</div>